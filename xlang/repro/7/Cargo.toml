[package]
name = "secp256k1-repro"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "secp256k1-repro"
path = "src/main.rs"

[dependencies]
# Pinned EXACTLY. The advisory range is >=0.24.0,<0.24.2; 0.24.1 and the
# other vulnerable releases (0.22.1, 0.23.4) are yanked from crates.io, so
# 0.24.0 is the only fetchable vulnerable version.
secp256k1 = "=0.24.0"
