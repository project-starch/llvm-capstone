{
  "case": 2,
  "upstream_fix": "106d0761c0",
  "title": "ap_request_core_filter's brigade must carry EOR past the request that made it",
  "consumer": "server/protocol.c, ap_request_core_filter()",
  "live_in_pin": null,
  "live_proof": "this corpus pins the allocators (APR 1.7.4, apr-util 1.6.3), not httpd; a case reproduces the call sequence rather than a shipped tree, and says so instead of asserting liveness",
  "object": "the brigade holder itself, allocated from the request pool",
  "lifetime_ender": "apr_pool_destroy of the request pool at the end of the request",
  "shape": "a pool outlived by its holder or its contents",
  "allocator_layer": "apr_bucket_alloc_t over apr_pool_t over apr_allocator_t -- two recycling levels, neither reaching malloc",
  "fidelity": "model-consumer, real allocators: apr_buckets_alloc.c and apr_pools.c are upstream byte for byte through the port's shims; brigades, filters, connections and requests are reduced to the holder and the storage",
  "arms": {
    "native-fix-differential": {
      "oracle": "the buggy arm leaves storage named after its owner gave it back; the fixed arm does not",
      "note": "this pair differs by whether the upstream fix is applied, not by protection",
      "measures": "free() is interposed and counted, so freed_to_malloc is measured per case and not inherited from the census"
    },
    "cheribsd-revocation": {
      "target": "stock CheriBSD purecap, libc heap revocation enabled",
      "cases": [
        "bucket-2-fixed",
        "bucket-2-buggy"
      ],
      "oracle": "both arms complete and print their native verdict unchanged: the storage never reached malloc, so it never entered the quarantine the revoker sweeps",
      "measured": "holder_pool=request holder_reissued=1 freed_to_malloc=0",
      "control": "the run's cheribsd-abi probe prints CHERI_ABI pointer_bytes=16 runtime_revocation=1 from CheriBSD's own malloc_revoke_enabled(), in the same guest, immediately before these cases",
      "guest_default": "preserved -- nothing is disabled to make room for this measurement",
      "runner": "runners/run-cheribsd.sh",
      "note": "freed_to_malloc is measured in the guest by the same interposed free() as the native arm, so the reason the revoker is idle is read off this run and not carried over from the census"
    },
    "spatial": {
      "status": "not written"
    },
    "sublet": {
      "status": "not written"
    },
    "poisoncap-spatial": {
      "status": "not written"
    },
    "poisoncap-protected": {
      "status": "not written"
    },
    "native-detect": {
      "status": "not written",
      "requires": "neither level reaches malloc, so ASan has no event; Valgrind against APR's own annotations is the arm that could discriminate"
    }
  },
  "status": "native pair run 2026-09-21; both passed. CheriBSD purecap with revocation on, same day: both arms complete, the revoker sees nothing. No Capstone, Sublet or PoisonCap arm exists: the allocators build freestanding but no domain workload does",
  "note": "the protected arms are unwritten because the port is a census plus this corpus, not a domain workload. The gap is declared rather than absent"
}
