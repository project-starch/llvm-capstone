{
  "case": 4,
  "upstream_fix": "152ddb68f7",
  "title": "an unlocked reference-count decrement drifts below the true number of holders",
  "consumer": "proto_parser.c, process_mget_cmd()'s error path",
  "live_in_pin": false,
  "live_proof": "the fix is in the pinned tree: process_mget_cmd's error path calls do_item_remove only when of.locked, and item_remove -- which takes the lock -- otherwise. The case reconstructs the pre-fix path, which called do_item_remove either way",
  "object": "an item, i.e. one chunk of a slab class, held by two clients at once",
  "lifetime_ender": "do_item_remove (items.c:538) when the drifted count reaches zero: item_free -> slabs_free, and the chunk goes on its class's slots list, never to free()",
  "shape": "unlocked refcount update / count drifts below the holders / slabs reuse / read through the dead pointer",
  "allocator_layer": "slabs.c size classes over 1 MiB pages, memcached 1.6.45",
  "fidelity": "model-consumer, real allocator: slabs.c is upstream's file at the 1.6.45 pin and makes every allocation decision. The item layer is reduced to item_free and do_item_remove; the lost update is written out as the three steps of refcount_decr (memcached.h:1041) with the concurrent get landing between them, because that interleaving IS the defect and a single -- would hide it",
  "distinguishing": "the count here is neither overwritten (case 2) nor overflowed (case 3): it is simply one short of the truth, and nothing in the allocator or the item layer can tell. The free that follows looks correct at every step",
  "arms": {
    "spatial": {
      "mode": 0,
      "oracle": "the sequence completes: report.completed == 1, no fault, the case marker printed",
      "role": "control for sublet: same binary and guest, one difference. A chunk keeps the alias it was carved with, so the holder's pointer still names live storage -- the next item's -- and the read succeeds"
    },
    "sublet": {
      "mode": 1,
      "oracle": "fault at the labelled read probe mc_defect_read, and nowhere else: exactly one fault, pc equal to the address the run's own marker published",
      "cause": 24,
      "note": "release and issue each revoke the chunk, so the holder's pointer is dead by the time it is read"
    },
    "poisoncap-spatial": {
      "mode": 0,
      "oracle": "the sequence completes: exit 0, report status=0 completed=1 count=1 mode=0, no fault observed by the supervisor, and the adapter's own line reports sweeps=0",
      "role": "control for poisoncap-protected: bounded leases and no invalidation, so the freed chunk stays addressable and the stale read succeeds"
    },
    "poisoncap-protected": {
      "mode": 1,
      "oracle": "fault at the labelled read probe mc_defect_read, observed from outside: SIGPROT with si_code PROT_CHERI_TAG, and the faulting address and the PC both equal to the address the supervisor resolved from the child's ELF and memory map",
      "signal": 34,
      "si_code": 2,
      "note": "si_code 2 and not 3: the sweep has run and the holder's alias is revoked, which is a different refusal from an access to a poisoned but unswept granule"
    },
    "cheribsd-revocation": {
      "target": "stock CheriBSD purecap: slab pages from the platform's own malloc, its kernel-defaulted libc revocation on",
      "mode": 0,
      "oracle": "the sequence completes: exit 0, report status=0 completed=1 count=1 mode=0, no fault observed by the supervisor",
      "control": "revocation-control in the same boot, before the case: free a block, sweep, read through the old pointer at the same labelled clbu -- must fault with SIGPROT si_code=PROT_CHERI_TAG at pc == mc_defect_read; with --runtime-revocation off it must complete instead",
      "guest_default": "preserved -- the kernel default stays on; the common runner sets the per-process policy explicitly",
      "note": "item_free ends in slabs_free, which pushes the chunk on its class's slots list; the chunk never reaches free(), so libc's quarantine never holds it"
    },
    "native-detect": {
      "status": "not written",
      "requires": "the chunk never reaches free(), so ASan has no event; it would see the reuse only as an ordinary live object"
    },
    "native-fix-differential": {
      "oracle": "the buggy arm loses the concurrent get inside the unlocked decrement, so the next release frees an item still held and the next slabs_alloc returns the same chunk; the fixed arm serialises the two and the count never reaches zero",
      "note": "unit_reissued differs between the arms, as in cases 2 and 3"
    }
  },
  "status": "native pair 2026-09-21 through the port, both arms as expected (buggy: unit_reissued=1, accessed_through_stale=1, damage=1, chunk_reuses=1; fixed: all zero). Capstone domain 2026-09-21: spatial completes, sublet faults at mc_defect_read cause=24 at the published address; negative control fires. PoisonCap 2026-09-21: spatial completes with sweeps=0, protected faults with SIGPROT si_code=PROT_CHERI_TAG at the resolved address, behind the platform's five instruction controls; negative control fires. Stock CheriBSD 2026-09-21: completes with revocation on and verified, reporting chunk_reuses=1 -- the chunk really was freed and reissued -- while revocation-control faults in the same boot; with revocation off it completes too; negative control fires",
  "note": "the upstream message calls the effect 'refcount drift on a busy item' leading to 'memory corruption', and says it is inert unless someone is constantly sending overlarge opaques; this records the corruption step, not its likelihood"
}
