{
  "case": 0,
  "upstream_fix": "461fb22053",
  "title": "af_join tracks an input buffer by the channel index instead of nb_buffers",
  "consumer": "libavfilter/af_join.c, try_push_frame()",
  "live_in_pin": false,
  "live_proof": "the fix's removed line is present verbatim in n8.0, n8.1 and n8.1.1 and absent from n8.1.2 onwards, so it is absent from the 9.0.1 pin; the pin governs the fidelity tier, not whether the fixture builds",
  "object": "an input frame's plane buffer, reached by av_frame_get_plane_buffer",
  "lifetime_ender": "the last reference is dropped when the input frames are freed, returning the payload to the pool",
  "shape": "reference never taken / reuse / stale read",
  "allocator_layer": "AVBufferPool, through avcodec_default_get_buffer2 (libavcodec/get_buffer.c:196)",
  "fidelity": "model-consumer, real allocator: buffer.c is upstream's, the dedup loop is transcribed, and the filter graph and the part of AVFrame it touches are reduced",
  "distinguishing": "the only case here where the defect is an absent reference rather than a retained pointer",
  "arms": {
    "native-fix-differential": {
      "oracle": "the buggy arm reads the reissued storage at the same address; the fixed arm holds a reference so nothing is reissued",
      "note": "this pair differs by the upstream fix, not by protection"
    },
    "spatial": {
      "probe_case": 36,
      "oracle": "the sequence completes"
    },
    "sublet": {
      "probe_case": 36,
      "oracle": "fault at the labelled read probe",
      "cause": 24
    },
    "cheribsd-revocation": {
      "target": "stock CheriBSD purecap, libc revocation enabled",
      "case": "pool-0-36",
      "oracle": "the sequence completes: the storage never reached malloc, so it never entered the quarantine the revoker sweeps",
      "control": "the run's cheribsd-abi probe reports runtime_revocation=1 from CheriBSD's own malloc_revoke_enabled(), in the same guest, immediately before these cases",
      "guest_default": "preserved -- unlike the PoisonCap runs, this one does not disable the kernel default"
    },
    "poisoncap-spatial": {
      "mode": 0,
      "case": "pool-0-36",
      "oracle": "the sequence completes, exit 0"
    },
    "poisoncap-protected": {
      "mode": 2,
      "case": "pool-2-36",
      "oracle": "SIGPROT after the setup marker FF2_PROBE case=36 ready",
      "exit": 162,
      "note": "CheriBSD reports a process status, not a fault PC, so the marker is what separates this from an arbitrary crash: it is printed only once reuse at the same address has been checked"
    },
    "native-detect": {
      "status": "not written",
      "requires": "the port's payload arena is one allocation, so ASan's silence here would measure the fixture rather than FFmpeg"
    }
  },
  "status": "native pair, both domain arms, both PoisonCap arms and the stock-CheriBSD arm run 2026-09-20; all passed. The PoisonCap run carried its five platform controls; the CheriBSD run carried the revocation control that proves the revoker was enabled while the case completed"
}
