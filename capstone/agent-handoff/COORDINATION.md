# COORDINATION — live ownership between the two agents

Single source of truth for **who owns what right now**. Update this file
*before* you start on a subsystem/submodule and *when you hand off*. Read it at
the start of every session. Rules behind it: `MULTI-AGENT-WORKFLOW.md`.
Setup for the second agent: `AGENT-B-SETUP.md`.

- **Agent-A** — clone `/home/alexey/dev/llvm-capstone`, branch `capstone-bootstrap`.
- **Agent-B** — clone `/home/alexey/dev/llvm-capstone-b`, branch `capstone-bootstrap-b`.

---

## Active ownership  _(edit before you start / on handoff)_

| Owner | Subsystem | Submodules / dirs held | Branch |
|---|---|---|---|
| Agent-A | (current: SQLite Stage-2 corpus, firmware/monitor) | `caplifive-buildroot` → `opensbi`, `capstone-sbi-domain/capstone-sbi`; `capstone/capstone-c`; `capstone/benchmarks/sqlite`; `capstone/tests` | `capstone-bootstrap` |
| Agent-B | compiler/codegen + emulator (C1 subobject-bounds proposal, design-only) | `llvm/` (in-tree) + `capstone/capstone-qemu` | `capstone-bootstrap-b` |

Suggested non-overlapping split (pick when B's task is set):
- Compiler/codegen (`llvm/`, in-tree) + `capstone/capstone-qemu` — one owner.
- Firmware/monitor (nested `caplifive-buildroot`/`opensbi`/`capstone-sbi`) + `capstone-c` — the other.
- `capstone/paper` — whoever is writing; low build-contention, coordinate edits here.

## Current position  _(update at EACH checkpoint — one line per agent; makes takeover read-and-go)_

One sentence each: what you're mid-doing, where, `branch@sha`, tested?, any
uncommitted WIP. This is the field the surviving agent reads to take over a lane
if the other hits a usage limit. Keep it honest and current.

| Agent | Current position |
|---|---|
| Agent-A | idle at `capstone-bootstrap` tip; no in-flight task; nothing uncommitted. |
| Agent-B | CHECKPOINT (2026-07-13, task 015): **CHERI baseline for the corpus measured — the empirical CHERI column of the security table (paper "Task 1").** Built the full CHERI-RISC-V **purecap** stack out-of-tree via cheribuild (`~/cheri-ws`, **NOT** in `llvm/`): CHERI-LLVM clang 17, CheriBSD purecap FreeBSD 15-CURRENT (`CHERI_CAPREVOKE`), `qemu-system-riscv64cheri` 7.1.0. Ran each corpus `before.c` purecap under **3 revocation configs** — **spatial** (revoke off), **temporal** (async default, `runtime_revocation_default=1`), **eager** (`+ every_free`) — each confirmed per-process via `malloc_revoke_enabled()`. **KEY FINDING (confirms the thesis):** spatial-only CHERI blocks **3/15** (only null-derefs 12/13/14, synchronous tag fault); the realistic **async-default temporal blocks 0/10 use-after-free at the contract point** (the dangling cap is revoked only by a later sweep, never synchronously at `step`/`reset`/`finalize`/`close`) — it adds only the double-free (row 11, caught by the **allocator's abort**, not a sweep); revoke-every-free blocks all shim rows but is non-default/expensive; and the **real diesel reuse-not-free case (`row3_reuse`) is blocked by NO config** (stale-but-allocated: cap stays tagged + in-bounds → the clean "CHERI-can't" headline). **METHODOLOGY NOTE the paper MUST carry:** upstream SQLite does **not** run purecap standalone here — `SIGBUS`/`BUS_ADRALN` (misaligned cap) in `sqlite3_open` at `THREADSAFE=0`, deadlock at `THREADSAFE=1`, for BOTH the vanilla amalgamation and CheriBSD's patched `contrib/sqlite3` 3.46.1 — so each shim is compiled **VERBATIM** against a minimal SQLite-lifecycle harness (`mock-sqlite/`) reproducing the exact alloc/free/callback/invalidation events (harness runs clean purecap: `sanity_mock`=0, real-sqlite `sanity_vanilla`/`sanity_clean`=138), built **`-O0`** so the UB dangling access is actually emitted. **Additive, measurement only: NO `llvm/` change, NO `capstone-qemu` change, NO gitlink bump** — only new files under `capstone/tests/cheri-baseline/`. RESULTS + config reality: `tests/cheri-baseline/RESULTS.md`; trail `history/13-07-2026_20-45-00_cheri-corpus-test.md`. No *our-QEMU* rootfs lock (CHERI has its own QEMU). Nothing uncommitted after this push. PREVIOUS (task 014): **paper deliverable 2 (borrow-path cost) measured.** Vehicle (a) instruction-count proxy on the QEMU functional model, confirmed with the human at the gate. Added emulator readout **`csrdicount`** to `capstone-qemu` (reads `icount_get_raw()`; `-icount shift=0` ⇒ 1 tick = 1 retired instruction) — **gitlink bumped `dd97f994`→(new), submodule pushed FIRST**. New `tests/runtime-qemu/borrow-cost-probe/`: one domain measures three variants of the same borrow-one-word op — **raw = 2 instr/op (O(1)); capability borrow (`mrev`+`delin`+access+`revoke`, inline in-domain) = 6 instr/op = +4 over raw, payload-independent (O(1)); defensive copy = `2+payload/8` instr (O(payload)): 34 @256B, 130 @1024B**. So borrow **stays close to raw (3.0×, +4 instr)** and is **well below copy (5.7×–21.7× cheaper @256B–1024B; gap grows with payload)**. Dynamic `csrdicount` counts match the `-O2` disassembly exactly. **LABEL any paper number a functional-model instruction-count proxy, NOT cycle-accurate timing** (QEMU has no pipeline/cache/cycle model; a timing claim needs an FPGA/gem5 vehicle not in-tree). Additive `--qemu-extra-arg` passthrough in `run-domain-smoke.py` (default empty, existing probes unaffected). No `llvm/` change; A's repros/`start.S`/monitor/allocators/`capstone-c` untouched. RESULTS: `tests/runtime-qemu/borrow-cost-probe/RESULTS.md`; trail `history/13-07-2026_16-52-21_borrow-path-perf.md`. One QEMU boot; **rootfs lock RELEASED**. Nothing uncommitted; pushed to `origin/capstone-bootstrap-b`. PREVIOUS (task 013): row-12 provenance citation-mismatch resolved (docs-only; see sync log). PREVIOUS (task 010): **every in-scope Stage-2 shape now has a LITERAL real-SQLite matched pair, not just a mechanism probe.** Before: only row 3 (task-008 B2) was literal. Now 5 rows (one per class): **L row 11** (`run-sqlite-row11.sh` — 2nd `sqlite3_finalize` derefs the revoked stmt, 4/4 -O0/-O2 cause 24 + control), **U row 14** (`run-sqlite-row14.sh` — pre-open read of a genuine UNINIT `db` faults cause 26 self-proving; correct control opens with real `sqlite3_open` first, 4/4), **H row 7** (`run-sqlite-row7.sh` + `revoke_on_free_hier_alloc.h` + Phase-0 `tests/runtime-qemu/hier-revoke-probe` 6/6 — per-connection sub-arenas, statement allocations are SPLIT descendants of the connection's MREV'd node, close revokes the subtree so `sqlite3_step` on the child faults, sibling connection survives; **mechanism answer YES, intra-domain, no monitor change**; 4/4 real-SQLite cause 24, cause-25 self-proving in the primitive), **S row 2** (`run-sqlite-row2.sh` — real SQLite invokes a UDF whose freed `pApp` context is revoked, callback faults, 4/4 cause 24). **R row 19 skipped (documented):** SQLite reuses the column-text storage in place on the next step, no free event to hook; row 3 covers R. **Two residuals for the PI:** (U) real `sqlite3_open` allocates a fresh object so the UNINIT `db` is minted to model the uninitialised connection — the correct path runs real SQLite; (S) the callback-CONTEXT UAF is faithful single-domain, but the SEAL PROPER (a sealed capability on a DOMAIN-CROSSING call boundary) needs SQLite dispatch to cross into a separate callback domain (`__seal`/`__domcall`) — reported, not built. **Additive-only: no `llvm/`, no `capstone/capstone-qemu` (no gitlink bump), shared `revoke_on_free_alloc.h` unmodified; A's `sqlite_row3{,_b2}_domain.c`/`start.S`/monitor/`capstone-c` untouched.** Regressions: Capstone lit **39/39**; the 5 pairs (18 boots) + Phase-0 hier probe (6 boots) green; existing revoke-on-free-probe re-run green. Note `history/10-07-2026_16-30-00_faithful-matched-pairs-per-shape.md`; `stage2-mapping.md` gains a "Literal real-SQLite repro" column; each row README's "after" section updated. QEMU rootfs lock RELEASED. Nothing uncommitted; pushed to `origin/capstone-bootstrap-b`. PREVIOUS (task 009): **the SQLite Stage-2 corpus is COMPLETE — 17/17 in-scope rows validated on RTL** (was 15/17). Rows 11 (LINEAR, double-free) and 14 (UNINIT, use-before-init) closed as mechanism probes on RTL: 7 probes x `-O0`/`-O1`/`-O2`, **21/21 GREEN**, at `capstone/tests/runtime-qemu/linear-uninit-corpus-probe/` + `build-`/`run-linear-uninit-corpus-probe.sh`. Both blockers in the 2026-07-08 deferral note were removed by earlier B-lane work; **neither escape hatch it scoped out was needed** (no `share_uninit_region` monitor op, no gated `start.S` change, no firmware rebuild). (1) **row14's derivation is one missing `csdelin`** — `helper_csrevoke` (op_helper.c:709) types the retained handle from `cap_rev_tree_revoke()`'s `retain_data`: `LIN` if every revoked node was already delinearised, **`UNINIT`** if the lineage was still linear (cursor at end, the form `csinit` asserts on). So `revoke(mrev(arena))` on an un-`delin`'d grant yields a real UNINIT capability. Pre-`csinit` read -> FAULT **cause 26**; `csinit` then reads/writes the same bytes through the same handle. (2) **row14 needed an emulator fix and it is load-bearing**: `_helper_access_with_cap` never consulted the capability TYPE, so an UNINIT cap was denied only by bounds (cursor at end) and **a load at a negative, in-bounds offset returned the stale byte**. Spec: no load form permits type 3 (`Unexpected capability type (26)`); stores are permitted and left untouched. `RISCV_EXCP_UNEXP_CAP_TYPE = 0x1a` existed and was never raised — first user. Proven by rebuilding without it: `uninit_negative_offset_fault` **returned `0x14110000` instead of faulting**. (3) **row11**: `csdrop` (task 002) consumes the `cssplit`-carved statement handle; later deref -> FAULT 24, second `drop` -> FAULT 24 in `helper_csdrop`; `linear_no_drop_ok` is the control. **`csdrop` consumes a HANDLE, not memory** — `linear_drop_sibling_ok` shows the connection survives the drop and the host mmap sees the post-drop write. (4) **No expected cause moves with `-O`** here. Submodule bump **`fd4bc0c0`->`dd97f994`**, pushed before the gitlink bump. Docs: `stage2-mapping.md` -> 17/17; deferral note marked SUPERSEDED; trail in `history/10-07-2026_02-30-00_linear-uninit-rows-closed.md`. Regressions: revoke-matrix (monitor `revoke_region`, the only firmware source of UNINIT handles) PASS, sqlite borrow-revoke/hier-child/sealed-callback PASS, mrev-codegen 9/9, revoke-probes 4/4, task-007 matrix 16/16 (`-O0`/`-O2`). **FOR A:** `run-borrow-revoke-uaf-probe.sh` **fails and it is NOT task 009** — byte-identical output with the new check compiled out. Its runner still demands `NO-TRAP-GAP use-after-revoke store landed`, but round 2's store no longer reaches the lender: it **executes and is silently discarded**, no fault. Causes predate this task (revocation enforcement `3d71d161` 2026-06-30; UNINIT cursor-at-end `cf541a1f` 2026-07-04). A's probe + A's runner; B's lane rules forbid editing them. A silently discarded store is a worse failure mode than either alternative. The task-007 revoked-region host landmine is also unchanged and still A's call. Nothing uncommitted; nothing outstanding to push. PREVIOUS: CHECKPOINT (2026-07-09, task 007): **row3 Option B proven on the real delivery path — single domain, monitor-granted linear cap, intra-domain revoke, cached alias faults.** 8 probes x `-O0`/`-O1`/`-O2`, **24/24 GREEN**, landed at `capstone/tests/runtime-qemu/intra-domain-mrev-revoke-probe/` + `build-`/`run-intra-domain-mrev-revoke-probe.sh`. Additive only: **no** `start.S`, monitor, `capstone-c` or submodule change; no gitlink bump. (1) **Receive protocol** (the plan's step-0 question): there is no `lcc` index and no entry stub — `shared_region_annotated()` ends with `__domcallsaves(d, DPI_REGION_SHARE, r)` and `my_first_domain/start.S` already reloads that cap as `domain_main`'s FIRST argument. Existing runtime probes discard it because they are `.smode` payloads under the `sbi.dom` scaffold (cap parks in the scaffold's `regions[]`; S-mode derefs via ambient `cpmp`) and are built with Buildroot gcc, which has no capability builtins. This probe uses `domain_main` `.dom` images + `create_dom(path, NULL)`. Grant must be `REV_TRANSFERRED` + `PERM_INOUT` — `PERM_IN` makes `cstighten` silently delinearise the cap and `helper_csmrev` *asserts* `CAP_TYPE_LIN`. (2) **Cause asserted, opt-level dependent**: `-O0` spills the alias so the post-revoke reload clears the tag -> cause 24; `-O1`/`-O2` keep it in a register -> cause **25** (self-proving). Every cause-24 expectation carries a no-revoke control. (3) **task-006 confirmed load-bearing in situ**: C1 (the `-O1/-O2` payloads would not compile without it) and C2 (an unused `MREV` still survives `-O2`, `rd != x0`). (4) **GAP for A — after a domain revokes a `REV_TRANSFERRED` region, the HOST must not touch it.** The monitor keeps a stale duplicate in `regions[]` (its own TODO) and drops the `cpmp` entry, so the next host access misses, `swap_cpmp()` calls `cap_base(regions[id])` on an untagged reload, and `helper_cslcc: Assertion rs1_v->tag failed` **aborts QEMU**. Robustness gap, not a mechanism failure. Fixes: monitor should skip untagged `regions[]` in `swap_cpmp` + clear `regions[id]` on TRANSFERRED (A's lane); emulator's `lcc` assert could be a clean cause-24 fault (B's, small). Neither applied. (5) **Step-3 verdict — `MREV`-ing SQLite's own `memsys5` heap pointer is NOT reachable.** Pointing memsys5 at a granted linear arena works (it never does `inttoptr`; allocations are `&zPool[i*szAtom]`, frees use pointer differences), but `cincoffset` inherits the pool's `rev_node_id`, type AND bounds, so `MREV` of an allocation would sweep the whole heap — and cannot even run, since the pool must be `delin`'d (C3) and `csmrev` asserts `LIN`. `SPLIT` is the only fresh-node derivation and the emulator has **no merge op**, so a coalescing buddy allocator cannot be built on it. **Scaffold handed to A:** `probe_linear_arena.h` (carve one `SPLIT` sub-cap per protected value out of a separate arena) + `held_protected_value_lifecycle` (fault at 'finalize') + `held_arena_survives_revoke` (siblings and remainder survive). STOPPED before the matched-pair integration, per the task: the remaining call is corpus fidelity, A's lane. Note `history/09-07-2026_23-05-00_option-b-held-cap-probe-steps-1-3.md`. Nothing uncommitted; nothing outstanding to push. PREVIOUS: CHECKPOINT (2026-07-09, task 005): **intra-domain MREV/REVOKE codegen spike — ANSWERED, Option B mechanism is a normal probe build.** (1) A deref through a held cap after `REVOKE` **faults** — register-held (cause 25), across the C ABI (cause 25), spilled to memory (cause 24). No ambient re-materialisation: the `-O2` asm keeps the post-revoke load based on the gencap register. (2) `MREV` **retains** its linear source → one grant, many nested revocation handles. (3) A linear sub-cap is `MREV`-able via **SPLIT** (independent rev node); **SHRINK/SHRINKTO share the node** and are not a substitute. Landed 9 probes at `capstone/capstone-qemu/tests/capstone-mrev-codegen/` (GREEN 9/9, firmware-free `csdebuggencap` mint, driver asserts the exact fault cause). Submodule `e0cd45de`→`fd4bc0c0`, pushed; superproject gitlink bumped on `capstone-bootstrap-b`. Three codegen defects found, all B-lane, none blocking the mechanism: **C1** `CC_Capstone_FastCC` has no `MVT::i128` case → any `-O1+` domain TU with a non-inlined `static` fn taking a pointer ICEs clang; blocks the *SQLite integration*, ~10-line fix, deliberately NOT applied (shared LLVM tree). **C2** `cap_mrev`/`cap_delin` marked `Const`/`IntrNoMem` but mutate the rev-tree → unused MREV is DCE'd, two MREVs of the same value CSE into one. **C3** only NONLIN is copyable → `movc`/`cincoffset` null a LINEAR source, so passing a LINEAR cap by value consumes it silently. **Option B recipe for A:** `arena=<linear grant>; R=mrev(arena); alias=delin(arena); …use alias…; revoke(R)` — the `delin` is required (C3) and safe (alias stays revocable; REVOKE returns a reusable LIN handle). The arena must be reachable **only** through the tracked cap. Nothing uncommitted; nothing outstanding to push. Note `history/09-07-2026_20-42-10_intra-domain-mrev-codegen-spike.md`. PREVIOUS: CHECKPOINT (2026-07-09, task 004): **revoke probes landed in-submodule + csdrop durability**. Committed the four task-003 probes into the submodule's own tree `capstone/capstone-qemu/tests/capstone-revoke-probes/` (sources + `csrevoke_probe.h` + `run-revoke-probes.sh` reusing the sibling `capstone/tests/runtime-qemu` harness + README with mechanism/table/provenance-constraint). Submodule commit `e0cd45de` on top of `2e6a67d1`; superproject gitlink bumped `2e6a67d1`→`e0cd45de` on `capstone-bootstrap-b`. Driver GREEN 4/4 (mem-alias→fault cause-24, reg-alias→fault cause-25, unrelated→0x22130033, control→0x2214005E). **DURABILITY CONFIRMED — nothing outstanding to push.** Both are on the shared remotes: `origin/capstone-bootstrap-b` = `d2e9dd43` (superproject) and `e0cd45de` (`capstone-qemu`). Verified by a fresh clone of `project-starch/capstone-qemu` outside the working clone (tip `e0cd45de`, parent `2e6a67d1`, csdrop present, all 7 probe files present). A can `git submodule update` and integrate csdrop + probes into canonical. Agent-B now holds its own fine-grained PAT (`GIT_CONFIG_GLOBAL=~/.agent-creds/gitconfig-b`) and pushes superproject + submodule branches directly — push `capstone-bootstrap-b` ONLY, submodule branch before the gitlink bump. Note `history/09-07-2026_16-09-42_task004-...md`. PREVIOUS (task 003): **`csrevoke` memory-alias sweep VALIDATED — outcome (a)**. Answered the load-bearing question for BORROW-REVOKE rows 3/13/18/19: `csrevoke` invalidates **memory-resident** aliases of a revoked cap, not just the register operand. Mechanism = lazy revocation keyed on a shared rev-tree node; `rev_node_id` is preserved through `cap_compress`/`cap_uncompress` (bits 33-63), and a reloaded revoked cap comes back untagged (`helper_reg_set_cap_compressed`), so its deref faults (cause 24). Proven firmware-free by hand-minting a LINEAR cap via the `csdebuggencap` debug op (`.insn r 0x5b,0x1,0x40`) + `__builtin_capstone_cap_mrev`/`_revoke`. 4 probes under QEMU: `revoke_mem_alias`→FAULT cause-24 (the key: memory alias faults post-revoke), `revoke_reg_alias`→FAULT, `revoke_unrelated_ok`→ok 0x22130033 (no over-broad sweep), `revoke_mem_control`→ok 0x2214005E (isolates REVOKE as cause). Earlier R-probe "NO-TRAP-GAP" is orthogonal (provenance: borrower's cap wasn't a tracked descendant), NOT a sweep bug. **No capstone-qemu code change** (outcome a) → no gitlink bump this task. Full note: `history/09-07-2026_15-33-23_csrevoke-memory-alias-sweep-validated.md`. For A/PI: the QEMU revoke vehicle is proven; the only remaining gate for rows 3/13/18/19 is A's `start.S` linear authority, which must deliver the region **through the tracked linear cap** (not an SBI-query mapping). Probe sources in scratchpad (cross-lane to commit into A's `capstone/tests`). PREVIOUS (task 002): `csdrop` (DROP) implemented in `capstone-qemu` — the LINEAR / Stage-2 row-11 QEMU unblock. Submodule `capstone-qemu` bumped `cf541a1f`→`2e6a67d1` (branch `capstone-bootstrap-b`); superproject gitlink bumped on `capstone-bootstrap-b`. Pieces (all additive, in-lane): decode entry (Func3=001/Func7=0001011), `DEF_HELPER_2(csdrop)`, `trans_csdrop` (rs1-only, like `trans_csrevoke`), `helper_csdrop` (spec-faithful, type-agnostic — clears rs1 tag → next deref raises cause-24 UNEXP_OP_TYPE). Rebuilt `qemu-system-riscv64` (`CC=/usr/bin/gcc`, `-j 80`). VALIDATED under QEMU: control `csdrop_live` → ok (retval 0x220F005E); fault `csdrop_use_after` → read live cap ok, drop, re-read → "Cap mem access requires capability" cause=24, no retval (consume→use→clean fault, NOT illegal instruction). No regressions: canonical smoke + borrow-revoke UAF (R) both pass. Full note: `history/09-07-2026_13-28-31_csdrop-implemented-row11-qemu-unblock.md`. NOT pushed yet (awaiting go-ahead; superproject branch + submodule commit both need pushing for A to integrate). Row 11 full before→after domain demo still GATED ON A: needs intra-domain linear authority (sign-off-gated `start.S`/firmware, Blocker 1). Test probe sources in session scratchpad (not committed — would be a cross-lane add to A's `capstone/tests`). Prior C1 v1 (arrays-only subobject bounds) merged to canonical by A (ff to `c4758de`). PREVIOUS: IMPLEMENTED C1 subobject-bounds v1 (sign-off received). UNCOMMITTED WIP on disk in `capstone-bootstrap-b` (proposal `da765ce` committed): clang flag `-fcapstone-subobject-bounds` (LangOptions.def, Options.td), CGExpr.cpp `maybeNarrowSubobjectBounds` hook (v1=array fields only + union/FAM/last-array/incomplete refusals), lit `clang/test/CodeGen/capstone-subobject-bounds.c`, authority probes `capstone/tests/capstone-authority/domains/subobjfield_{overrun,inbounds,union_active,flexarray}.c` + oracle + build-script case. Reconfigured B's cmake tree cleanly per PI recipe: host compiler `/usr/bin/clang;/usr/bin/clang++` (NEVER a capstone-built clang), Debug/shared, `X86;RISCV;Capstone`, `clang;lld`, opt-tablegen, bindings off. Build capped at `-j 80` (~71% of 112 cores — never all CPUs) DONE (exit 0). COMPILER VALIDATION GREEN: driver forwards the flag, new lit PASS, clang-Capstone lit 7/7, llvm-Capstone backend lit 36/36, IR narrows to [field,field+sizeof) as intended. RUNTIME authority suite: human OK'd building the full stack in B (commit held until runtime probes pass). BUILT (standard host toolchain, `-j 80`, nice'd, serialized): capstone-qemu DONE (qemu-system-riscv64 v8.1.1 — note: had to force `CC=/usr/bin/gcc`; configure auto-picked a build-folder trunk clang from PATH that miscompiled op_helper.c). capstone-c DONE. buildroot DONE (fw_jump.elf/Image/rootfs.ext2 built). RUNTIME AUTHORITY VALIDATION GREEN (5/5): subobjfield_overrun→bounds-fault (the flip: field over-read now traps), subobjfield_{inbounds,union_active,flexarray}→ok (in-field works; union+FAM refusals hold, no false trap), subobject_overread (no flag)→no-trap-today (gap still in default compiler). Full increment validated: lit 7/7 + backend lit 36/36 + authority 5/5. Compiler changes ready to commit (awaiting human go-ahead). Compiler changes validated + uncommitted pending runtime pass. NOTE: buildroot build is Agent-A's firmware lane — running per explicit PI authorization; no firmware submodule gitlink bumped. Next: lit + authority-suite validation, then report for commit approval. NOTE cross-lane: added additive files under `capstone/tests` (A's held dir) per explicit PI task — see sync log. |

## Claimed / do-not-touch  _(hold list)_

| Path or submodule | Held by | Until |
|---|---|---|
| `capstone/agent-handoff/state/current-state.md` + `current-next-step.md` | Agent-A (base files) | ongoing — Agent-B uses `*.B.md` |
| QEMU `rootfs.ext2` write-lock (one guest boot at a time across A+B) | **Agent-A — 2026-07-14** — validating rtl-smoke plumbing under QEMU + Capstone-side perf (baseline vs revoke-on-free) | until this session's QEMU runs finish (will mark RELEASED) |
| _add entries as you claim exclusive edit rights_ | | |

## Submodule-bump log  _(append after every gitlink change — this is where conflicts hide)_

| Date | Agent | Submodule | Old→New SHA | Superproject commit |
|---|---|---|---|---|
| 2026-07-08 | Agent-A | (main repo) added `capstone/paper` @ db9142f (opt-in, update=none) | — → d4959767 | d4959767d27c |
| 2026-07-09 | Agent-B | `capstone/capstone-qemu` (implement `csdrop`/DROP — row-11 QEMU unblock) | cf541a1f → 2e6a67d1 | (superproject commit on `capstone-bootstrap-b`) |
| 2026-07-09 | Agent-B | `capstone/capstone-qemu` (add `tests/capstone-revoke-probes/` — task-003 revoke probes) | 2e6a67d1 → e0cd45de | d2e9dd43 on `capstone-bootstrap-b`; both pushed, durability verified by fresh clone |
| 2026-07-09 | Agent-B | `capstone/capstone-qemu` (add `tests/capstone-mrev-codegen/` — task-005 codegen spike probes) | e0cd45de → fd4bc0c0 | on `capstone-bootstrap-b`; submodule branch pushed before this bump |
| 2026-07-10 | Agent-B | `capstone/capstone-qemu` (deny loads through an UNINIT cap → cause 26; name the double-`drop` fault in the log) | fd4bc0c0 → dd97f994 | on `capstone-bootstrap-b`; submodule branch pushed before this bump |

## Sync log  _(append at each integration point)_

| Date | Action | Detail |
|---|---|---|
| 2026-07-08 | scaffolding | Added `MULTI-AGENT-WORKFLOW.md`, `AGENT-B-SETUP.md`, this file; paper submodule committed (`d4959767`). Push required for Agent-B's clone to pull them. |
| 2026-07-08 | Agent-B online | Agent-B came online in clone `/home/alexey/dev/llvm-capstone-b` on `capstone-bootstrap-b`. Verified isolation: `CAPSTONE_REPO_ROOT`/`CAPSTONE_CLANG` inside B clone, `CAPSTONE_TMP_ROOT=/tmp/capstone-b`, `CLAUDE_CONFIG_DIR=~/.claude-b` (own creds), remote=project-starch/llvm-capstone, clean tree. Created `state/current-{state,next-step}.B.md`. LLVM build + buildroot/rootfs not yet built (fresh clone). Awaiting task assignment; no submodule owned. |
| 2026-07-08 | Agent-B merge + lane claim | Merged `origin/capstone-bootstrap` (`39f68da`, agent-unavailable resilience + checkpoint protocol) into `capstone-bootstrap-b` (clean, no conflict). Claimed the **compiler/codegen + emulator** lane (`llvm/` in-tree + `capstone/capstone-qemu`); no firmware submodule touched. Task: design-only C1 subobject-bounds proposal (`design/c1-subobject-bounds-proposal.md`), gated for review before any implementation or full build. |
| 2026-07-14 | **FOR AGENT-B (codegen lane): new `-O2` Capstone-backend ICE** | While validating the rtl-smoke FPGA port under QEMU, A hit a **deterministic clang backend segfault** (`909c8722`, `+assertions`, exit 139 in RunCodegenPipeline) at `-O2` only (`-O0`/`-O1` fine). **Minimal trigger:** a *conditional store of a capability value into two distinct named globals* — `if (first) g_a = arg; else g_b = arg;` where `g_a`/`g_b` are `static void *` and `arg` is a delivered capability. Bisected: not the null-test, not any surrounding loop, not the store's later use. **Workaround** (used in the port, no LLVM change): an array-indexed store `regions[i++ & 1] = arg` compiles clean. Repro in `capstone/tests/rtl-smoke/borrow_cost_fpga.c` (revert the `regions[]` array to two named globals + conditional store). Trail: `history/14-07-2026_17-39-48_rtl-smoke-qemu-validation.md`. A did **not** touch `llvm/` — this is B's lane to root-cause/fix. |
| 2026-07-09 | Agent-B csrevoke sweep validated | Task 003: validated (no code change, outcome a) that `csrevoke` sweeps **memory-resident** aliases, not just the register operand — the load-bearing property for BORROW-REVOKE rows 3/13/18/19. Lazy rev-node scheme; `rev_node_id` survives compress/uncompress; reloaded revoked caps come back untagged. Proven firmware-free via `csdebuggencap` (`.insn`) + mrev/revoke builtins; 4 probes under QEMU (mem-alias FAULT, reg-alias FAULT, unrelated OK, mem-control OK). No `capstone-qemu` bump. History note `09-07-2026_15-33-23_...`. For A: revoke vehicle proven; remaining gate is A's `start.S` linear authority, which must route the region **through the tracked linear cap** (the R-probe's provenance lesson). Probe sources in scratchpad (cross-lane if promoted to `capstone/tests`). |
| 2026-07-09 | Agent-B csdrop shipped | Implemented `csdrop` (DROP) in `capstone-qemu` (row-11 LINEAR QEMU unblock, task `agentB-002`). Submodule bump `cf541a1f`→`2e6a67d1` on submodule branch `capstone-bootstrap-b`; superproject gitlink + this file + `state/*.B.md` + history note committed on `capstone-bootstrap-b`. Additive only (new opcode, previously illegal); no existing instruction changed; no firmware submodule touched. Validated under QEMU (control ok + fault=cause-24 clean cap-fault) with no regressions (smoke + borrow-revoke UAF pass). **Integration:** A merges B→canonical as with C1 v1 — but A also needs the `capstone-qemu` commit `2e6a67d1` reachable (push submodule branch to shared `project-starch/capstone-qemu`, or A fetches from B's clone). Row-11 full domain demo still needs A's gated linear-authority `start.S`. |
| 2026-07-09 | Agent-B task-004 (durability + probes) | Landed the four task-003 revoke probes in the submodule's own tree `capstone/capstone-qemu/tests/capstone-revoke-probes/` (in-lane; not `capstone/tests`). Submodule commit `e0cd45de` (on `2e6a67d1`); superproject gitlink bumped `2e6a67d1`→`e0cd45de` + docs on `capstone-bootstrap-b`. Driver reuses the runtime-qemu harness; GREEN 4/4 on binary `2e6a67d1`. **Durability CONFIRMED:** superproject `d2e9dd43` and `capstone-qemu` `e0cd45de` are both on `origin/capstone-bootstrap-b`; proven by a fresh clone of the submodule remote outside the working clone (tip `e0cd45de`, parent `2e6a67d1`, csdrop + all 7 probe files present). **Integration for A:** `git submodule update` resolves the gitlink from `project-starch/capstone-qemu` origin — no `.gitmodules`/origin change was made. Nothing outstanding to push. |
| 2026-07-09 | Agent-B task-005 (intra-domain MREV codegen spike) | Answered the one codegen unknown left by A's Option-B firmware spike. **(1) Deref through a held cap after REVOKE FAULTS — the revoke is not missed.** Verified on three paths (register-held, across the C ABI, spilled to memory); `-O2` asm shows the post-revoke load still based on the gencap register (`lbu a1, 8(a1)`), no ambient re-materialisation. **(2) MREV RETAINS its source** (`helper_csmrev` never nulls rs1) → one linear grant mints many nested revocation handles. **(3) A linear sub-cap is MREV-able via SPLIT** (fresh rev node ⇒ siblings revoke independently); **SHRINK/SHRINKTO are NOT a substitute** — they copy `rev_node_id`, so the sub-cap shares the arena's node. Landed 9 probes in-submodule at `tests/capstone-mrev-codegen/` (GREEN 9/9, firmware-free via `csdebuggencap`); bump `e0cd45de`→`fd4bc0c0`. **Three codegen defects found, all B's lane, none blocking the Option-B *mechanism*: C1** `CC_Capstone_FastCC` lacks the `MVT::i128` case → any `-O1+` domain TU with a non-inlined `static` fn taking a pointer ICEs clang (`CapstoneISelLowering.cpp:23820`); blocks the *SQLite integration*, ~10-line fix, NOT applied (shared LLVM tree, A also builds it). **C2** `cap_mrev`/`cap_delin` are marked `Const`/`IntrNoMem` but mutate the rev-tree → at `-O2` an unused MREV is DCE'd and two MREVs of the same value CSE into one. **C3** only NONLIN is copyable, so `movc`/`cincoffset` null a LINEAR source: passing a LINEAR cap by value consumes it, silently, no diagnostic. **For A — the Option B recipe:** `arena=<linear grant>; R=mrev(arena); alias=delin(arena); …use alias…; revoke(R)`. The `delin` is required (C3) and is safe (delin only clears the node's `linear` flag; the alias stays revocable and REVOKE returns a reusable LIN handle). Arena must be reachable **only** through the tracked cap — an ambient/`gp`-reachable buffer has a second, un-revocable path (`mrev_ambient_miss` returns instead of faulting). **Bottom line: Option B's mechanism is a normal probe build; no firmware, no codegen fix needed.** Note `history/09-07-2026_20-42-10_intra-domain-mrev-codegen-spike.md`. |
| 2026-07-09 | Agent-B credentials | Agent-B received its own fine-grained PAT (`GIT_CONFIG_GLOBAL=~/.agent-creds/gitconfig-b`, via the `claude-b` wrapper), scoped `Contents: R/W` on `llvm-capstone`, `capstone-qemu`, `pointer-safe-paper`, `caplifive-buildroot`. B now pushes superproject **and** submodule branches itself — no operator hand-off, no out-of-band fetches from B's clone. Discipline (self-enforced; A and B share one GitHub identity): B pushes **only** `capstone-bootstrap-b`, never `capstone-bootstrap`/`main`; submodule branch is pushed **before** the superproject gitlink bump. Gotcha: `capstone-spec` is deliberately out of token scope — a recursive fetch may error `could not read Username ... capstone-spec`; use `--no-recurse-submodules`. |
| 2026-07-09 | Agent-B C1 subobject v1 shipped | PI approved implementing. Landed `-fcapstone-subobject-bounds` (clang frontend, default off, arrays-only v1) + lit + authority `subobjfield_*` probes. Validated: clang lit 7/7, backend lit 36/36, runtime authority 5/5. **Cross-lane note:** added ADDITIVE files under `capstone/tests/capstone-authority/` (A's held dir) — new `subobjfield_*.c` domains + appended `oracle.tsv` lines + one additive case in `build-authority-suite.sh`; no existing probe/oracle line changed, no submodule gitlink bumped. Built the full runtime stack in the B clone (capstone-qemu, capstone-c, buildroot) with standard host toolchain (`/usr/bin/{gcc,clang++}`) at `-j 80` — did NOT modify any firmware submodule. No commits pushed; on `capstone-bootstrap-b` only. |

---

### Handoff etiquette
- Before bumping a submodule: check the **Claimed** list; if someone holds it, wait or coordinate here.
- After bumping: append to the **Submodule-bump log** in the same commit.
- Keep the branches close — merge B→A at checkpoints rather than letting gitlink SHAs diverge for weeks.
- Durable facts both agents need go in a committed `agent-handoff/` doc, **not** only in an agent's private memory (memory doesn't cross clones/accounts).

### Resilience / agent-unavailable (full protocol in `MULTI-AGENT-WORKFLOW.md`)
- **Commit + push small and often** (exact paths). Uncommitted WIP in a dead session is the only thing a usage-limit cutoff can actually lose.
- **Update your `state/*.md` and your Current-position line** at every checkpoint, so the other agent can take over your lane read-and-go.
- Lanes are independent: one agent hitting its limit never blocks the other's lane — it degrades two lanes to one, never to zero.
- If the integrator (A) is dark and B needs a merge: B keeps committing to `capstone-bootstrap-b`; integration waits or the human temporarily promotes B to merge B→canonical, and A reconciles submodule bumps from the log on return.
- The human can `git commit` a stalled clone's WIP at any time, even while that agent is dark.
| 2026-07-09 | Agent-B row3 Option B probe | Task 007: landed the single-domain held-cap BORROW-REVOKE probe (`capstone/tests/runtime-qemu/intra-domain-mrev-revoke-probe/` + two top-level scripts). **Cross-lane add under A's `capstone/tests`, per the explicit lane exception in the task-007 brief** — a NEW isolated dir only; no existing probe, harness file, `start.S`, or monitor file touched. 8 probes x 3 opt levels, 24/24 green. Additive only; no `start.S`, monitor, `capstone-c` or submodule change, so **no gitlink bump**. Two findings A owns: the host must not touch a region the domain has revoked (`swap_cpmp` aborts QEMU via `helper_cslcc`), and `MREV` of SQLite's own `memsys5` allocations is unreachable (`cincoffset` shares the pool's rev node; `SPLIT` has no inverse). Scaffold for the pragmatic path: `probe_linear_arena.h`. Note `history/09-07-2026_23-05-00_option-b-held-cap-probe-steps-1-3.md`. |
| 2026-07-09 | Agent-A integrated B lane + built row3 B1 | Merged `origin/capstone-bootstrap-b` → canonical `capstone-bootstrap` (`a3e3e007`): C1 (fastcc+cap-arg), C2 (MREV/DELIN purity), task-005 codegen probes, task-007 held-cap probe; gitlink `e0cd45de`→`fd4bc0c0`. Rebuilt clang, verified C1/C2 in situ (both lit tests fail on stale compiler, pass on rebuilt; Capstone lit 38/38). Then built the **row3 B1 matched pair** (`benchmarks/sqlite/sqlite_row3_domain.c` + `sqlite_host_row3.c` + `run-sqlite-row3.sh`; parameterised `build-sqlite-{capstone,host}.sh`): real SQLite, single domain, revoke at `sqlite3_finalize`, post-finalize read faults. **GREEN at -O0** (cause 24 + no-revoke control reads `'c'`); -O2 asm proves the register-held alias; **-O1/-O2 QEMU blocked** by a domain-TU codegen bug (`helper_csdelin: rd_v->tag` — `.insn`-split cap loses tag across a call spill; aborts the control too, not the mechanism) → routed to B (task-008). Additive; no `start.S`/monitor/`capstone-c`/submodule change beyond the merged gitlink. QEMU rootfs lock used + released. Note `history/09-07-2026_23-15-00_row3-b1-matched-pair.md`; plan `plans/sqlite-row3-matched-repro-plan.md` ("B1 built + validated"). New B task drafted (008): literal fork B2 = revoke-on-free non-coalescing linear allocator. |
| 2026-07-10 | Agent-B closed corpus rows 11 + 14 | Task 009: **the SQLite Stage-2 corpus is complete — 17/17 in-scope rows validated on RTL** (was 15/17). 7 probes × `-O0`/`-O1`/`-O2`, **21/21 green**, at `capstone/tests/runtime-qemu/linear-uninit-corpus-probe/` + `build-`/`run-linear-uninit-corpus-probe.sh`. Both blockers in the 2026-07-08 deferral note were removed by B-lane work, and **neither escape hatch it scoped out was needed** — no `share_uninit_region` monitor op, no gated `start.S` change, no firmware rebuild. **(1) row14 (UNINIT)**: the derivation is one missing `csdelin`. `cap_rev_tree_revoke()` returns `retain_data`; `helper_csrevoke` (op_helper.c:709) makes the retained handle `LIN` when every revoked node was already delinearised, and **`UNINIT` when the lineage was still linear** (cursor at end — the form `csinit` asserts on). So `revoke(mrev(arena))` on an un-`delin`'d grant hands the domain a real UNINIT capability. Pre-`csinit` read → FAULT **cause 26**; `csinit` (models `sqlite3_open`) then reads/writes the same bytes through the same handle. **(2) row14 needed an emulator fix, and it is load-bearing.** `_helper_access_with_cap` never consulted the capability **type**: an UNINIT cap was denied only incidentally, by bounds (cursor at end), so **a load at a negative, in-bounds offset returned the stale byte** — the disclosure UNINIT exists to prevent. Per spec no load form permits type 3 (`existing-insn.adoc` "Load Instructions"; `mem-access-insn.adoc` `[#load-cap]`: types 0/1/5 only, else `Unexpected capability type (26)`); stores *are* permitted (that is how an owner initialises memory) and are deliberately untouched. `RISCV_EXCP_UNEXP_CAP_TYPE = 0x1a` was defined and never raised — this is its first user. Proven load-bearing by rebuilding without it: `uninit_negative_offset_fault` **returned `0x14110000` instead of faulting**. **(3) row11 (LINEAR)**: `csdrop` (task 002) consumes the `cssplit`-carved statement handle; a later deref → FAULT cause 24, and a second `drop` (the literal double-`finalize`) → FAULT cause 24 inside `helper_csdrop`. `linear_no_drop_ok` is the control for both. **`csdrop` consumes a HANDLE, not memory** — it clears one register, does not revoke a lineage and does not free bytes; `linear_drop_sibling_ok` shows the connection the statement was carved from still works after the drop, and the host mmap reads back the post-drop write. **(4) No expected cause moves with `-O`** (unlike the held-cap probe): the UNINIT handle keeps its tag and a valid rev node; the dropped handle stays untagged through a spill/reload. **Submodule bump `fd4bc0c0`→`dd97f994`** (UNINIT-load check + a `DROP requires capability` diagnostic); pushed before this gitlink bump. **No** `start.S`, monitor, `capstone-c`, buildroot or LLVM change. Docs updated: `benchmarks/sqlite/cve-repros/stage2-mapping.md` (validation table → 17/17) and the deferral note `history/08-07-2026_13-01-23_...` marked SUPERSEDED; full trail in `history/10-07-2026_02-30-00_linear-uninit-rows-closed.md`. **Regressions:** revoke-matrix (the monitor's `revoke_region` — the only firmware source of UNINIT handles) PASS, sqlite borrow-revoke / hier-child-revoke / sealed-callback PASS, `tests/capstone-mrev-codegen` 9/9, `tests/capstone-revoke-probes` 4/4, task-007 intra-domain matrix 16/16 at `-O0`/`-O2`. **FOR A — two findings that are not mine to fix.** (a) `run-borrow-revoke-uaf-probe.sh` **fails**, and it is **not** caused by task 009: with the new check compiled out the output is byte-identical. Its runner still demands the marker `NO-TRAP-GAP use-after-revoke store landed`, but round 2's store no longer becomes visible to the lender (`round 2 returned 0xfa017ed`, `word after round 2 = 0x1111…`). The store *executes* and is *silently discarded* — the domain does not fault. Plausible causes both predate this task: revocation enforcement (`3d71d161`, 2026-06-30) and the UNINIT cursor-at-end placement (`cf541a1f`, 2026-07-04). The probe and its runner are A's; task-007's lane rules forbid B editing them. Worth A's attention: a silently discarded store is a worse failure mode than either a landed store or a fault. (b) The task-007 revoked-region host landmine is unchanged and still needs A's call (`swap_cpmp()` → `cap_base(regions[id])` on an untagged reload aborts QEMU). **Runner hardening (B's own files):** both `run_ok`s now retry a boot that dies before the domain returns — such a boot leaves no `call retval` line at all, so retrying it cannot mask a real failure. That flake spuriously failed `held_arena_survives_revoke` once at `-O2`. |
| 2026-07-10 | Agent-B row3 B2 literal matched pair | Task 008, on **separate branch `capstone-b-008-revoke-on-free`** (per the task). Closed the row3 fidelity gap A's B1 left: B1 revoked a carved COPY of the column name; **B2 makes SQLite's ENTIRE heap a revoke-on-free linear allocator** (`benchmarks/sqlite/revoke_on_free_alloc.h`, via `SQLITE_CONFIG_MALLOC`), so the pointer that faults post-`finalize` is the exact value `sqlite3_column_name` returned -- SQLite's own pointer, into SQLite's own allocation, revoked by the `xFree` on SQLite's own finalize path. No wrapper, no copy, no driver revoke. **Route 1** (non-coalescing allocator, NO emulator change). Each allocation is a fresh `SPLIT` sub-cap (own rev node), `MREV`'d while LIN, handed out `delin`'d NONLIN; `xFree` revokes it. **Phase 0** (allocator primitive, `tests/runtime-qemu/revoke-on-free-probe/`): 3 probes x `-O0/-O1/-O2`, 9/9. **Phase 1** (real SQLite, `benchmarks/sqlite/run-sqlite-row3-b2.sh`): fault + no-revoke control x `-O0/-O1/-O2`, **6/6** -- control returns colname 'c' live+post-finalize, fault variant's post-finalize `name[0]` read faults (cause 24 at `-O0`, **cause 25 self-proving at `-O1/-O2`**); fault PC maps into `run_row3_b2`'s read, badaddr in the arena, so it is the literal read not an internal SQLite free-then-use. **The `-O1+` self-proving boot is NEW** -- it required fixing the `-O1+` codegen bug that blocked B1. **CODEGEN FIX (llvm/, helps B1 too):** B1's `-O1/-O2` `helper_csdelin` abort was NOT a `.insn`/SPLIT tag loss (A's hypothesis) -- reduced to 12 lines, no `.insn`. Root cause is task-006 C2: a global base is `cincoffset gp+off` (LINEAR) + tied `delin`; C2 made DELIN side-effecting but `cincoffset` stays pure, so MachineCSE hoists the shared base and leaves a LINEAR value with multiple uses, and the first `movc` copy nulls it (C3). Fix: emit the pair as ONE `PseudoCapGlobalBase` (NONLIN result, no multi-use LINEAR value). Lit `global-base-nonlinear.ll`. **Second bug B2 forced:** `xRealloc` must copy **tag-preserving** (a byte-copy strips caps SQLite stored in reallocated structs -> untagged deref). **Phase 2 (generality, honest):** B2 is bounded-lifetime, NOT a general heap -- 100 prepare/finalize cycles carve 570 KiB for a 30 KiB peak (~19x non-coalesce overhead); 100000 cycles exhaust the emulator's fixed **10000-node revocation tree** (a hard `_cap_rev_tree_dup_node_before` ASSERT, not graceful NOMEM). General answer = a rev-tree merge op (**Route 2**), left as a design direction. **Regressions:** Capstone lit 40/40, runtime authority 32/32, Phase 0 9/9, Phase 1 6/6. Additive only; A's `sqlite_row3_domain.c` untouched; NO emulator/monitor/firmware/`start.S`/`capstone-c` change; `capstone-qemu` gitlink unchanged (`dd97f994`). **FOR A:** the rev-node-pool exhaustion should be a graceful fault not an assert (B emulator lane, semantics call, not done); Route 2 wants a design note first. Note `history/10-07-2026_06-40-00_row3-b2-literal-revoke-on-free.md`. Branch is separate -- A reviews/integrates; not merged to `capstone-bootstrap-b`. |
| 2026-07-10 | Agent-B task-010 faithful pairs per shape | Delivered a LITERAL real-SQLite matched pair for every in-scope Stage-2 shape (was: only row 3). **L row 11** double-finalize (`sqlite_row11_domain.c`, `run-sqlite-row11.sh`): 2nd `sqlite3_finalize` derefs the revoked stmt (rof allocation freed by the 1st finalize); 4/4 -O0/-O2 cause 24 + no-revoke control. **U row 14** uninit connection (`sqlite_row14_domain.c`, `run-sqlite-row14.sh`): `db` is a genuine UNINIT cap (revoke a still-linear SPLIT sub-cap), pre-open read faults cause 26 self-proving; correct control opens with real `sqlite3_open` first; 4/4. **H row 7** cursor dealloc (`sqlite_row7_domain.c` + `revoke_on_free_hier_alloc.h` + Phase-0 `tests/runtime-qemu/hier-revoke-probe` 6/6): per-connection sub-arenas — statement allocations are SPLIT descendants of the connection's MREV'd senior node; `hier_close` (fired at the zombie `sqlite3_close_v2`) revokes the subtree so `sqlite3_step` on the child faults; a sibling connection survives (scoping). **Mechanism answer: YES, hierarchical ownership is expressible intra-domain (SPLIT+MREV+REVOKE), no monitor/region/`start.S` change.** 4/4 real-SQLite cause 24; cause-25 self-proving reached in the primitive. **S row 2** UDF context UAF (`sqlite_row2_domain.c`, `run-sqlite-row2.sh`): real SQLite invokes a registered UDF whose freed `pApp` context is revoked, the callback faults; 4/4 cause 24. **R row 19 skipped (documented):** SQLite reuses the column-text storage in place on the next step, no free event; row 3 covers R. **Residuals for A/PI:** (U) `sqlite3_open` allocates fresh, so the UNINIT `db` is minted to model the uninitialised connection; (S) the callback-CONTEXT UAF is faithful single-domain, but the SEAL PROPER needs a domain-crossing seal into a separate callback domain — not built. **Additive-only: no `llvm/`, no `capstone/capstone-qemu` (no gitlink bump), shared `revoke_on_free_alloc.h` unmodified.** Regressions: Capstone lit 39/39; existing revoke-on-free-probe re-run green; 24 task-010 boots green. Note `history/10-07-2026_16-30-00_faithful-matched-pairs-per-shape.md`; `stage2-mapping.md` + row READMEs updated. On `capstone-bootstrap-b`, pushed. |
| 2026-07-10 | Agent-B task-012 HIERARCHICAL family literal across languages | Promoted two more HIERARCHICAL-REVOKE rows to literal real-SQLite matched pairs, in two more bindings, so the family now has THREE literal reprs across CPython (row 7, task-010) + Ruby + PHP — same intra-domain `revoke_on_free_hier_alloc.h` sub-arena cascade, distinct real CVEs. Generalization breadth, not a new mechanism. **row 9 (Ruby, sqlite3-ruby #49)** `sqlite_row9_domain.c` + `run-sqlite-row9.sh`: faithful `sqlite3Ruby`⊃`sqlite3StmtRuby` wrappers, `REQUIRE_OPEN_STMT` checks only NULL; Database teardown revokes the sub-arena, the child's 2nd `sqlite3_finalize` faults on the revoked `sqlite3_stmt`. **row 5 (PHP, #69971)** `sqlite_row5_domain.c` + `run-sqlite-row5.sh`: faithful `php_sqlite3_db_object`(with `zend_llist free_list`)⊃`php_sqlite3_stmt_object`, wrong destruction order; the Zend wrapper objects live on the SURVIVING global arena (they model a separate heap) so only SQLite's connection+statement are revoked; the statement free handler's `sqlite3_finalize(stmt->stmt)` faults INSIDE finalize on SQLite's own handle. **Both faithful to the fidelity bar:** the faulting deref is inside `sqlite3_finalize` on the `sqlite3_stmt` (verified: both fault at `pc=0x10200edd8`, vaddr `0x1edd8` ∈ `sqlite3_finalize` `[0x1ed7c,0x1ef90)`, `badaddr` in the connection sub-arena). **Cause 24 at -O0 AND -O2** (the deref is inside the always-`-O0` `sqlite3_finalize`, handle spilled → untagged reload); each carries a no-revoke control (zombie close → finalize/step returns). **Sibling scoping proven on REAL SQLite** (`-D*_SIBLING`, -O2): revoke connection A, `sqlite3_step` connection B → SQLITE_ROW, B survives — new vs task-010, which proved scoping only in the Phase-0 primitive. **Fidelity correction (in the note):** row 5's first cut put the PHP wrappers in the revoked sub-arena and at -O2 faulted on the revoked WRAPPER field (cause 25, wrong object); moved the Zend wrappers to the surviving arena so the fault lands on the `sqlite3_stmt`. Validated `OPT_LEVELS="-O0 -O2"`: row 9 5/5, row 5 5/5. **Additive-only: no `llvm/`, no `capstone/capstone-qemu` (no gitlink bump); `revoke_on_free_hier_alloc.h`, `sqlite_row7_domain.c`, A's `sqlite_row3*`/`sqlite_row2*`/`start.S`/monitor/`capstone-c` untouched; A's `cve-repros/row*/before*.c`+`oracle*`+`run-host-asan-repros.sh` untouched (only row 5/9 README "after" sections + `stage2-mapping.md` H literal column).** Regressions: Capstone lit 39/39; shared allocator unmodified so row 7 + Phase-0 `hier-revoke-probe` unaffected. **Nothing needed from A/PI** — pure intra-domain SPLIT+MREV+REVOKE, as task-010 predicted for the whole family. Note `history/10-07-2026_19-40-00_hier-family-literal.md`. QEMU rootfs lock claimed + released. On `capstone-bootstrap-b`, pushed. |
| 2026-07-13 | Agent-B task-013 row-12 provenance citation-mismatch | Closed the corpus's last known provenance liability. A's ledger flagged **row 12** (expo-sqlite null-deref) for citing PR **#34992** — a statement-*leak* fix, not the `SQLITE_BUSY`-ignored null-deref the row describes. Thorough upstream search (expo, cordova-sqlite-storage, better-sqlite3, node-sqlite3, PHP sqlite3, go-sqlite3, SQLite CVE DB) found **no** binding that ignores `SQLITE_BUSY`, nulls its C `sqlite3 *`, and derefs it in C: every real artifact either surfaces `SQLITE_BUSY` as a handled return code (no deref), crashes in the managed/runtime layer (expo Java NPE, go nil panic), or is an engine-internal null-deref (CVEs). **Outcome B (documented model) + citation re-anchor:** re-anchored the citation from the leak-fix PR to the real crash report **expo #34990** (a `java.lang.NullPointerException` on `closeSync()` close-while-busy, the artifact #34992 fixes), which documents this row's shape. Row 12 stays honestly **△ modeled** because #34990's fault is a **managed-language NPE**, not a C SQLite-pointer null-deref — same honest category as rows 7 (managed-dict GC) and 13 (host `row_factory` deletion). The C repro (`sqlite3_get_autocommit(NULL)` on the cleared handle) is a faithful behavioral translation, not forced to ✓ literal. Fault stays **null-deref** (not converted to UAF); the failed-close→no-revoke fidelity point preserved. False "All links verified" exposure removed for row 12; tally unchanged (4 modeled: 7, 12, 13, 16). **Docs-only, additive: no `llvm/`, no `capstone/capstone-qemu` gitlink bump, no code/emulator touched** (Capstone lit / shared allocators / rows 3/7 / `hier-revoke-probe` unaffected by construction), no QEMU boot (no rootfs lock needed). Edited row 12's own `PROVENANCE.md`/`README.md`/`NOTE.md` + shared `PROVENANCE-LEDGER.md` (row-12 line + "Resolved item"); `stage2-mapping.md` unchanged (never claimed row 12 literal). A owns the `tab:scope` caption; the ledger is now true. Note `history/13-07-2026_01-16-03_row12-provenance.md`. On `capstone-bootstrap-b`, pushed. |
| 2026-07-13 | Agent-B task-014 borrow-path cost (paper deliverable 2) | Measured the deferred deliverable-2 claim ("borrow path stays close to raw pointers and well below the copy baseline"). **Vehicle (a) instruction-count proxy on the QEMU functional model — confirmed with the human at the gate** (a real timing number needs an FPGA/gem5 vehicle not in-tree). Added emulator readout **`csrdicount`** to `capstone-qemu` (opcode `0x5b`/funct3 `0x1`/funct7 `0x48`; reads `icount_get_raw()`; `translator_io_start` makes the read accurate; under `-icount shift=0` a delta is an exact dynamic instruction count, readable at any privilege unlike `rdcycle`/`rdinstret`). **`capstone-qemu` gitlink bumped — submodule branch pushed FIRST, then the bump** (my lane). New `tests/runtime-qemu/borrow-cost-probe/`: one domain receives a real LINEAR arena and measures three variants of the same borrow-one-word boundary op, each bracketed by `csrdicount`, minus an empty calibration loop — **raw = 2 instr/op (O(1)); capability borrow (`mrev`+`delin`+access+`revoke`, threading the reclaimed handle, inline in-domain) = 6 instr/op = +4 over raw, payload-independent (O(1)); defensive copy (fair 16-byte memcpy) = `2 + payload/8` instr (O(payload)): 34 @256B, 130 @1024B**. Result: borrow **stays close to raw (3.0×, +4 instr)** and is **well below copy (5.7× cheaper @256B, 21.7× @1024B; gap grows with payload)**. Dynamic counts match the `-O2` disassembly exactly (2/6/34/130 — deterministic). **The monitor/ioctl path was excluded by design** (it would measure Linux syscall scaffolding, an emulation artifact; the paper's mechanism is the inline capability ISA sequence). **LABEL any paper number a functional-model instruction-count proxy, not silicon timing.** Additive `--qemu-extra-arg` passthrough in `run-domain-smoke.py` (default empty; existing probes unaffected). No `llvm/` change; A's row2/3/5/7/9/11/14 repros, `start.S`, monitor, shared allocators, `capstone-c` untouched. RESULTS `tests/runtime-qemu/borrow-cost-probe/RESULTS.md`; trail `history/13-07-2026_16-52-21_borrow-path-perf.md`. One QEMU boot (`-snapshot`); rootfs lock released. On `capstone-bootstrap-b`, pushed. |
| 2026-07-13 | Agent-B task-015 CHERI corpus baseline | Produced the empirical **CHERI column** of the paper's security table. Built the full CHERI-RISC-V **purecap** stack out-of-tree via cheribuild (`~/cheri-ws`, not vendored into `llvm/`): CHERI-LLVM clang 17, CheriBSD purecap FreeBSD 15-CURRENT (`CHERI_CAPREVOKE`), `qemu-system-riscv64cheri` 7.1.0. Each corpus `before.c` compiled purecap and run under **3 revocation configs** (spatial / async-default temporal / eager-every-free; each confirmed with `malloc_revoke_enabled()`). **Result:** spatial-only blocks 3/15 (null-derefs only); **async-default temporal blocks 0/10 use-after-free at the contract point** (+ double-free via the allocator abort) — confirming CHERI temporal safety is async, not synchronous at `step`/`reset`/`finalize`/`close`; revoke-every-free blocks all shim rows (non-default, expensive); the **real diesel reuse-not-free case (`row3_reuse`) is blocked by NO config** (stale-but-allocated). **Methodology (paper must carry it):** upstream SQLite does **not** run purecap standalone here (`SIGBUS`/`BUS_ADRALN` in `sqlite3_open` at THREADSAFE=0; deadlock at THREADSAFE=1; both vanilla and CheriBSD-patched `contrib/sqlite3`), so the shims are compiled VERBATIM against a minimal SQLite-lifecycle harness (`mock-sqlite/`, runs clean purecap) at `-O0` (so the UB dangling access is emitted). **Additive, measurement only: no `llvm/` change, no `capstone-qemu` change, NO gitlink bump** — only new files under `capstone/tests/cheri-baseline/`. No *our-QEMU* rootfs lock. RESULTS `tests/cheri-baseline/RESULTS.md`; trail `history/13-07-2026_20-45-00_cheri-corpus-test.md`. On `capstone-bootstrap-b`, pushed. |
