#ifndef MC_POISONCAP_H
#define MC_POISONCAP_H
/* What the PoisonCap authority layer counted, printed by the hosted entry and
 * the example beside their ordinary report. Mode 0 must show no sweeps: that
 * is what makes it the spatial control rather than a second protected arm. */
void mcp_poisoncap_report(void);
#endif
