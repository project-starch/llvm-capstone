# Phase D, first --boot runs with typedef __uintcap_t Datum (intcap plan, 2026-09-25-intcap-implementation.md)
compiler: compiler/intcap 87e005a71c4e (Phases A, B and C), clang/llc from its Release+Asserts build
qemu: capstone-qemu-movc-merge d621df553f (CAPSTONE_MOVC_NULL_SCALAR off), buildroot snapshot br-snap, cma=640M
build: patches 0001-0013; 760 objects compiled; link rc=0, 0 undefined symbols; loadable size 81780240
run: run-domain.sh 4 (the --boot call over the catalog script), one attempt per run

gate 1 "past create pg_proc" (the uintptr_t Datum faulted here, at GetTableAmRoutine+0x38): PASSED
  debug4 log: "creating bootstrap relation pg_proc 1255" then "bootstrap relation created"

each run's stop, and what moved it:
  1 heap_fill_tuple -> memcpy, cause 24: att_align_nominal's char branch returned (uintptr_t)(ptr)   -> patch 0011
  2 FATAL "buffer io 0x1 is not owned by resource owner TopTransaction", after 84 pg_proc rows
      root cause: patch 0007's TYPEALIGN evaluated its operand twice; InitBufferPool passes
      ShmemInitStruct("Buffer Blocks", ...) as the operand, the second call reported found=true
      (logged: found descs=0 bufs=1 iocv=0 ckpt=0), buffer descriptors were never initialized
      (buf_id 0 and freeNext 0 everywhere; victim buffers all claimed id 0)                        -> 0007 rewritten
  3 read_stream_look_ahead, cause 24: cincoffset on a NULL per_buffer_data (+0)                      -> patch 0012
  4 FATAL 'could not stat directory "base/1": No file descriptors available'
      the musl-capstone runtime serves 8 open files (HC_MAX_FILES); raised to 128 on both sides
      as a MEASUREMENT only, not committed (a runtime change, its own branch and hostcall gates)
  5 XLogInsert (XLogRecordAssemble), cause 24: lhu 0xc(t5), t5 an untagged page-aligned pointer
      from regbuf->page. A tag probe in XLogRegisterBlock caught the page (0xd9d21000, untagged)
      with log_newpages as the caller: bulk_write.c sorts its pending pages with pg_qsort, whose
      void-element swap moves bytes and strips the tag                                           -> patch 0013
  runs between these sometimes hung instead of faulting (same image, fault on one run, no output
  on another), so the outcome of a failing image is not deterministic; the passing image passed
  every run with the 1800 s bound (1 run at a 1500 s bound was cut short before it printed)

call 4 (--boot over the catalog script): PASS, status=0, rounds=17902, in 2 runs (identical rounds)
  -- with the runtime's open-file table at 128 (stop 4): committed since on its own branch,
     runtime/hostcall-more-files 162ef1071fba (hostcall probes 28/28 before and after)

call 5 (--single over the setup SQL), chained from call 4's OWN output:
  a native recording cannot be the input: "database files are incompatible with server",
  "initialized with MAXALIGN 8, but the server was compiled with MAXALIGN 16" (the port's
  MAXIMUM_ALIGNOF 16, which predates the intcap work). So the plan's gate "a native postgres
  accepts the directory" cannot hold for this port and has to be restated as a domain-only chain.
  initdb itself writes base/1/PG_VERSION between the two calls; it is the one file the domain's
  call-4 output lacks against the native before-5 directory, and it is copied from there
  run-domain.sh put the extra -c options after --single's database name ("invalid command-line
  argument: -c"); they now go before it, for --single only (--boot ends in -X's value)

call 5 result: PASS, status=0, rounds=34187, 707 statements logged, the last two
  CREATE DATABASE postgres OID = 5 STRATEGY = file_copy and its COMMENT (the end of the setup SQL)
  image: patches 0001-0016, loadable size 83598240; its input: the catalog call 4 wrote with the
  0001-0013 image. So the two calls ran different images; the chain below is one image through both.

one image, patches 0001-0016 as committed (c1f876ca09c0), built from a fresh extraction:
  call 4 (--boot)                            PASS, status=0, rounds=17902
  call 5 (--single, on call 4's own output)  PASS, status=0, rounds=34186, 707 statements

work.sql (survey-native.sh's session: DDL, 2000 rows, an index, a scan, a join, an update, a
delete, a vacuum, a count) under --single in database postgres of that cluster, its result rows
against the native session's (work-compare.py; a fresh native initdb and --single on this host):
  QEMU                                  run 1 PASS, run 2 PASS: status=0, rounds=2587, 22 of 22
                                        result rows identical, the last count = "1500"
  QEMU, CAPSTONE_MOVC_NULL_SCALAR=1     run 1 PASS, run 2 PASS: status=0, rounds=2587, 22 of 22
  (the silicon's MOVC rule)             result rows identical. The switch was in effect: each
                                        boot printed its notice once (first nulling at M-mode
                                        pc 0x80020a50); the two runs without it printed none
  work-compare.py's controls: a native log against itself IDENTICAL; one value changed
  (1500 -> 1499) DIFFER, exit 1; an empty domain log "no result rows", exit 1
  one run (QEMU, run 2) printed its result and then stopped for 13 minutes before lt.user's exit,
  with no watchdog line either; killed. A guest whose timers no longer fire would look like
  this and like the mid-run stalls below; that is a hypothesis, not tested

the catalog call 4 wrote, against the native one, tuple by tuple: every difference is one the port
  makes on purpose -- 176 pg_proc rows 16 bytes longer (array data MAXALIGNed to 16), pg_type's
  pointer-sized pg_ddl_command and internal at typlen 16, 25 pg_class rows whose relpages follow

call 5's stops, and what moved each:
  356 statements  pg_import_system_collations popen()s "locale -a"; a domain cannot spawn  -> patch 0014
  358 statements  FATAL 'could not access file "$libdir/dict_snowball"': a domain cannot dlopen
                  -> patch 0015: the modules the setup SQL needs (dict_snowball, plpgsql) are linked
                  into the image, 2 modules and 240 functions, resolved by base name
  559 statements  FATAL "internal size 8 is invalid for passed-by-value type" (CREATE TYPE):
                  TypeCreate compares an 8-byte by-value size with sizeof(Datum), now 16
                  -> patch 0016, with the three other places that take sizeof(Datum) for a
                  by-value Datum's width: datum_image_hash, outDatum, readDatum
  695 statements  COPY ... FROM '<initdb's install>/share/postgresql/sql_features.txt', a host path
                  -> run-domain.sh moves such paths to the staged share (1 path in this recording)

repeatability, one image (0014 plus a diagnostic in relationHasPrimaryKey): 4 runs, 3 stopped at the
  same statement with identical rounds (7784), 1 stalled

events not explained:
  - once: FATAL 'multiple primary keys for table "pg_proc"' at statement 2, the first ADD PRIMARY KEY,
    on the image before that diagnostic. 7 later runs over 3 images passed the statement. The
    uninstrumented index_check_primary_key reads indisprimary at t_data + t_hoff + 14, as it should
    (its disassembly). One input to a single-threaded backend should give one answer: OPEN.
  - once: 100% CPU for 30 minutes at CREATE VIEW pg_roles (statement 262); killed
  - twice: a run stopped mid-way (statements 354 and 348), the vCPU in the guest kernel's idle task
    (same sp and tp over three samples 20 s apart), so neither the domain nor its host helper was
    runnable. run-domain.sh gained PGSU_STALL_DUMP=<seconds>, which prints the helper's thread
    states, wait channels and kernel stacks from inside the guest, for the next one. Its first
    version slept the whole period holding the pipe into sed, and a finished call 4 waited on it
    for 30 minutes (the guest's date read the same second at the start and end of the run); it
    now polls every 5 s and writes to /dev/console
  - twice: boots that never reached the domain (the loader stall of HOW-TO-RUN-ON-QEMU.md)
