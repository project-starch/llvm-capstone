#include <stdio.h>
/* Oracle for the QEMU arm (SEED=0): no tag is ever lost under emulation, so the sweep returns
   TAGSWEEP_OK with a zero count = 0xA5000000. */
int main(void){printf("2768240640\n");return 0;}
