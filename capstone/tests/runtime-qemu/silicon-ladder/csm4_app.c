/* csmith seed 4 of the cycle-2 campaign, as a silicon-ladder rung (QEMU half). */
#define CSM_PROGRAM "csm4_kernel.h"
#include "csm_ladder.h"
#include "../../../benchmarks/beebs/adapted/beebs_freestanding_string.c"
void domain_main(unsigned *res, unsigned func) { (void)func; *res = csm_compute(); }
