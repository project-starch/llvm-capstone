/* Freestanding math.h for csmith programs in a domain.  The campaign generates
 * with --no-float, so nothing here is ever called; csmith.h includes the header
 * unconditionally and a freestanding build has none. */
#ifndef CAPSTONE_FUZZ_MATH_H
#define CAPSTONE_FUZZ_MATH_H
#endif
